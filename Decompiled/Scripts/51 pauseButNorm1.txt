global gBonusRound

on mouseEnter
  if (sprite(749).locV > 600) or (sprite(749).locV < 0) then
    set the member of sprite the currentSpriteNum to member("pauseButOver1")
    sound(7).playFile(_movie.path & "sound\" & "buttonover1.wav")
  end if
end

on mouseUp
  put "GAH " & sprite(749).locV
  if (sprite(749).locV < 600) and (sprite(749).locV > 0) then
    sound(7).playFile(_movie.path & "sound\" & "warning.wav")
  end if
end
