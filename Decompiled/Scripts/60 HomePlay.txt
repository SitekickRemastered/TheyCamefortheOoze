global gNewChips

on mouseEnter
  if gNewChips = 0 then
    set the member of sprite the currentSpriteNum to member("HomePlayOver")
    sound(7).playFile(_movie.path & "sound\" & "buttonover1.wav")
  end if
end
