global gNewChips

on mouseEnter
  if gNewChips = 0 then
    sound(7).playFile(_movie.path & "sound\" & "buttonover1.wav")
    set the member of sprite the currentSpriteNum to member("HomeQuitOver")
  end if
end
